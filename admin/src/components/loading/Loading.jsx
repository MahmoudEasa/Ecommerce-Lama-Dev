import "./loading.css";
import { CircularProgress } from "@mui/material";

const Loading = () => {
  return (
    <div className="loadingPage">
      <CircularProgress />
    </div>
  );
};

export default Loading;
